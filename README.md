Start by installing and activating conda environment by running:
conda env create -f environment.yml

Load environment:
conda activate satellite_env

If on GPU enabled device:
pip3 install torch torchvision torchaudio
